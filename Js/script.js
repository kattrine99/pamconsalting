let cityCard = document.getElementById('Limassol');
function showInfo() {
    let city = document.getElementsByTagName('area');
    if(city == 'Limassol')
    {
        cityCard.style.display = 'flex';
    }
    
}